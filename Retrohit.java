public class Retrohit {
    private static int count = 0;

    public static void main(String[] args) {
        // Increment the counter each time the app is run
        count++;

        // Display the message with the current count
        System.out.println("Hello World from Retrohit! This is refresh number " + count);
    }
}
